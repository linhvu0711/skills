name: Repo check

on: [push, pull_request]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Look for leaks
        run: bash scripts/check.sh
      - name: Test the checks
        run: bash scripts/test.sh
