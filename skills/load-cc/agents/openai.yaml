interface:
  display_name: "Claude Code Session Loader"
  short_description: "Load a Claude Code session as a state brief."
  default_prompt: "$load-cc <session-id>"
policy:
  allow_implicit_invocation: false
