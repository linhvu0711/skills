<!-- embed-source: {lib}@{version} -->
# {Lib} {topic}

One-line statement of what this file covers and when to reach for it.

## Constructors

Short table or list: name, one-line purpose, file in `repos/{lib}/...` where it is defined.

## Shapes this project uses

Two or three snippets, each under 20 lines. Copy them from tests under `repos/{lib}`, then trim to the idiom. Name the source file above each snippet so the reader can open it.

```ts
// from repos/{lib}/packages/.../X.test.ts
```

## Mistakes to avoid

Three to five bullets. Each names the wrong shape and the right one. Prefer mistakes visible in this project's history or in the library's own tests.
